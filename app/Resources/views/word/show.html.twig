{% extends 'base.html.twig' %}

{% block body %}
    <h1>Word</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ word.id }}</td>
            </tr>
            <tr>
                <th>Value</th>
                <td>{{ word.value }}</td>
            </tr>
            <tr>
                <th>Count</th>
                <td>{{ word.count }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('word_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('word_edit', { 'id': word.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
